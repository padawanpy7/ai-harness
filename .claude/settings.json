{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty' 2>/dev/null); case \"$f\" in */skills/*.md) d=$(dirname \"$f\")/..; [ -f \"$d/scripts/skill-sync.sh\" ] && (cd \"$d\" && bash scripts/skill-sync.sh) >/dev/null 2>&1 || true ;; esac"
          }
        ]
      }
    ]
  }
}
